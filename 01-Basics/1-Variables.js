const num = 12345;
// We can not change CONST value

let username = "Abhinav";
//Use LET to define variables

var city = "Bijnor";
// We can use VAR too for defining variables but due to scope problem we do not use VAR anymore

state = "Uttar Pradesh";
// We also can define variables without using any keyword ( LET, VAR, CONST ) but it is not a good practice


console.log(num);
console.log(username);
console.log(city);
console.log(state);
// We use console.log() to show output in console

console.table([num, username, city, state]);
// We can use console.table() to show output in table format

