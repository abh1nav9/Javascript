"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Abhinav")

let age = 21
// 1. number => 2 to power 53
// 2. bigint

let name = "Abhinav"
// 3. string => ""

let isLoggedIn = false
// 4. boolean => true/false

let x = null;
// 5. null => standalone value ( NULL means Empty not Zero)

let state;
console.log(typeof undefined); // undefined
// 6. undefined ( Undefined means a variable has been declared but has not yet been assigned a value)

// 7. symbol => To make any value unique we us it

// 8. object
console.log(typeof null); // object